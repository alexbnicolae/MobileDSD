import { StyleSheet } from "react-native";

export const stylesIconButton = StyleSheet.create({
    buttonContainer: {
        borderRadius: 24,
        padding: 6,
        margin: 8
    },

    pressed: {
        opacity: 0.75
    }
})